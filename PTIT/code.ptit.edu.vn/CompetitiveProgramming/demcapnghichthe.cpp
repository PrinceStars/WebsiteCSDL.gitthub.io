#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin >> n;
    long arr[n];

    for (int i = 0; i < n; i++)
        cin >> arr[i];
    long long res = 0;
    vector<long> a;
    for (int i = n - 1; i >= 0; i--)
    {
        }
    cout << res << '\n';
    return 0;
}