#include <bits/stdc++.h>
using namespace std;
int arr[10];

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int n;
    long long m;
    cin >> n >> m;
    for (int i = 0; i < n; i++)
        cin >> arr[i];
    return 0;
}